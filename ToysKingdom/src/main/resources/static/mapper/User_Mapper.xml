<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mts.toyskingdom.mapper.UserMapper">

    <resultMap id="userMap" type="com.mts.toyskingdom.data.entity.UserE">
        <result column="id_user" property="idUser"/>
        <result column="email" property="email"/>
        <result column="password" property="password"/>
        <result column="full_name" property="fullname"/>
        <result column="phone_number" property="phoneNumber"/>
        <result column="address" property="address"/>
        <result column="birthday" property="birthday"/>
        <result column="role" property="role"/>
        <result column="active" property="active"/>
    </resultMap>

    <select id="getAllUser" resultMap="userMap">
        SELECT *
        FROM users
    </select>

    <select id="getUserByEmail" parameterType="String" resultMap="userMap">
        SELECT * FROM users WHERE email = #{email}
    </select>

    <select id="getUserByidUser" parameterType="int" resultMap="userMap">
        SELECT * FROM users WHERE id_user =#{idUser}
        </select>


    <insert id="insertUser" parameterType="com.mts.toyskingdom.data.dto.UserRegistrationDto">
        INSERT INTO users (email,full_name, password, phone_number, address, birthday)
        VALUES (#{email},#{fullname}, #{password}, #{phoneNumber}, #{address}, #{birthday})
    </insert>

    <update id="updateUser" parameterType="com.mts.toyskingdom.data.dto.UserRegistrationDto">
        UPDATE users
        SET full_name = #{fullname}, email = #{email}, password = #{password}, phone_number= #{phoneNumber}, address=#{address}, birthday=#{birthday}
        WHERE id_user  = #{idUser}
    </update>
    <delete id="deleteUser" parameterType="int">
        DELETE FROM users WHERE id_user = #{idUser}
    </delete>

</mapper>
